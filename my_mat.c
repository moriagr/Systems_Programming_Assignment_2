#include <stdio.h>
#include "my_mat.h"

int existingPath(int i, int j)
{
    printf("(i, j):  (%c, %c)\n", i, j);
    return 0;
}

int shortestCourse(int i, int j)
{
    printf("---(i, j):  (%c, %c)\n", i, j);
    return 0;
}